import { StudStylePipe } from './stud-style.pipe';

describe('StudStylePipe', () => {
  it('create an instance', () => {
    const pipe = new StudStylePipe();
    expect(pipe).toBeTruthy();
  });
});
